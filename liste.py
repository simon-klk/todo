
class Liste:
    def __init__(self):
        pass

    def add_liste(self):
        name = self.list_name.get()
        numElem = 0
        beschreibung = self.list_beschreibung.get()
        datum = self.task_date.get() # Erstelldatum (final)

        #new_list = 
        #self.root.destroy()
        #if self.refresh_callback:
        #    self.refresh_callback()